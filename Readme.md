# 说明

本脚本用于intel网卡X520/X540/X710的VF网卡配置

- 首次运行

直接执行：

```shell
git clone https://github.com/yishunzhikong/sriov_vf.git && cd ./sriov_vf && ./sriov_vf_manager.sh
```

后续直接进入"sriov_vf"目录执行下面命令就可以更新并运行了：

```shell
git pull && ./sriov_vf_manager.sh
```